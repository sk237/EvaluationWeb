request
    getParameter = get
    getParameterValues =  get 배열



response
    sendRedirect = 다른곳으로 보내기


cookie

Session
    기존 연결정보 저장
    getSession = 로그인 ?
    setAttribution =
    invalidate = 로그아웃?